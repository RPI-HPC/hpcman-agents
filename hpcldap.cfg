[connection]
host = localhost
dbuser = todora
database = hpcman
sslmode = require
sitename = TCTS
statedirectory = /tmp/hpcfoo
loglevel = debug
password_type = ssha

slave_listener_port = 60999

database_retry_interval = 60.

[universe]
uri = ldap://localhost
dn = dc=test,dc=rpi,dc=edu
admin = cn=admin,%(dn)s
password = secret
